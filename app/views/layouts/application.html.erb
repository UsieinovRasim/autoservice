<!DOCTYPE html>
<html>
  <head>
    <title>Autoservice</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="d-flex flex-column flex-md-row align-items-center pb-1 mb-3 border-bottom">
        <a href="/" class="d-flex align-items-center text-dark text-decoration-none">
          <svg xmlns="http://www.w3.org/2000/svg" width="40" height="32" class="me-2" viewBox="0 0 118 94" role="img"><title>Bootstrap</title></svg>
          <span class="fs-1">AUTO SERVICE</span>
        </a>

        <nav class="d-inline-flex mt-2 mt-md-0 ms-md-auto">
          <span class="me-4 py-2 text-dark text-decoration-none"><%= link_to "Главная страница", home_path %></span>
        </nav>
      </div>
    <div class="container-fluid pb-3">
      <div class="d-grid gap-3" style="grid-template-columns: 1fr 5fr;">
        <div class="bg-light border rounded-3">
          <%= render "layouts/sidebar_navigation" %>
        </div>
        <div class="main-container">
          <%# flash.each do |k, v| %>
            <%# k = %w[alert error danger].include?(k) ? 'danger' : 'success' %>
            <%#= tag.div v, class: "alert alert-#{k} alert-dismissible", role: 'alert' %>
          <%# end %>
        <%= yield %>
      </div>
    </div>
  </div>
  </body>
</html>
